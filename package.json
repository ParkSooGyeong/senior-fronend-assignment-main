{
  "name": "senior-frontend-assignment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter openai-api-mock dev\" \"pnpm --filter chat-app dev\"",
    "dev:mock": "pnpm --filter openai-api-mock dev",
    "dev:app": "pnpm --filter chat-app dev", 
    "build": "pnpm --filter chat-app build",
    "test": "pnpm --filter chat-app test",
    "lint": "pnpm --filter chat-app lint"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3",
    "concurrently": "^8.2.2"
  }
}
